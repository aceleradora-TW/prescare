<%- contentFor('body') %>
    <div class="botao-voltar">
        <input type="submit" value="Voltar"  class="form-control" onclick="window.location.href='/lista-acolhidos'">
    </div>

    <div class="cabecalho-telas">
        <%- include partials/cabecalhoTelas.ejs %>
    </div>

    <%if (user.tipo === "clinica" || user.tipo === "neurologista") { %>
        <div class="botoes">
            <input type="submit" value="Nova Prescrição"  class="form-control" onclick="window.location.href='/acolhido/<%= acolhido.id %>/prescricao/create'">
        </div>
    <% } %>

    <%- include partials/tabelaPrescricaoAcolhido.ejs%>

