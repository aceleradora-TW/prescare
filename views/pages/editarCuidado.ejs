<div class="medicamento">

    <p class="nome-acolhido"> Nome:
        <%= cuidado.prescricao.acolhido.nome %>
      </p>

  <form action="<%= updateUrl %>" method="POST">
    <h1>Cuidado</h1>
    <div>
      <label for="descricao">Descricao:</label>
      <input type="text" name="descricao" id="descricao" value="<%= cuidado.descricao %>">
    </div>

    <div>
      <label for="intervalo">Intervalo:</label>
      <input type="text" name="intervalo" id="intervalo" value="<%= cuidado.intervalo %>">
    </div>

    <div>
      <label for="observacoes">Observações:</label>
      <input type="text" name="observacoes" id="observacoes" value="<%= cuidado.observacoes %>">
    </div>


    <div class='botoes'>
      <form action="<%= updateUrl %>/Adicionar" method="POST">
        <input type="submit" value="Adicionar" onclick="alert('Cuidado adicionado')">
      </form>
      <form action="/cuidado/<%= cuidado.id %>/excluir" method="DELETE">
        <input type="submit" value="Excluir">
      </form>
      <form action="<%= updateUrl %>" method="POST">
        <input type="submit" value="Voltar" onclick="alert('Cuidado não adicionado')">
      </form>
    </div>
  </form>
</div>